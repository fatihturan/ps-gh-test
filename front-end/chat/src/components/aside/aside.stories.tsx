import type { Meta, StoryObj } from '@storybook/react';

import Aside from '.';

const meta: Meta<typeof Aside> = {
    title: 'Components/Phase I/Aside',
    component: Aside
};

export default meta;
type Story = StoryObj<typeof Aside>;

export const AsideStory: Story = {};
