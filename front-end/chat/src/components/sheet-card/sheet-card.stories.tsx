import type { Meta, StoryObj } from '@storybook/react';

import SheetCard from '.';

const meta: Meta<typeof SheetCard> = {
    title: 'Components/Phase II/SheetCard',
    component: SheetCard
};

export default meta;
type Story = StoryObj<typeof SheetCard>;

export const SheetCardStory: Story = {};
